{
  "name": "react-minimal",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "./node_modules/.bin/opener http://localhost:1234/ && ./node_modules/.bin/parcel serve index.html",
    "build": "./node_modules/.bin/parcel build index.html",
    "clean": "echo \"Cleaning project ...\" && rm -Rf dist .cache"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "opener": "latest",
    "parcel": "latest"
  }
}